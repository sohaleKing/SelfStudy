//the same as Object.keys() but returns the valus

const object = { name: "soheyl", age: 30, student: true };

const arr_values = Object.values(object);
console.log(arr_values); //=> [ 'soheyl', 30, true ]
